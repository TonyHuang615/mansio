services:
  lociterm:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - lociterm-data:/data
      - lociterm-home:/home/lociterm
    restart: unless-stopped

volumes:
  lociterm-data:
  lociterm-home:
